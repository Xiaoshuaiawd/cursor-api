version: '3'

services:
  cursor-api:
    image: wisdgod/cursor-api:v0.3.4-3
    container_name: cursor-api
    restart: unless-stopped
    ports:
      - "3000:3000"  # Assuming the API runs on port 3000, adjust if needed
